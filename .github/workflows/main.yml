name: 'Bot de Normas'
on:
  schedule:
      - cron: '0 11 * * *'
    workflow_dispatch:
    jobs:
      run-bot:
          runs-on: 'ubuntu-latest'
              steps:
                    - run: 'python main.py'
                    
